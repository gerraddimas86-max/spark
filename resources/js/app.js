// Import Alpine.js (default Breeze)
import Alpine from 'alpinejs';

window.Alpine = Alpine;
Alpine.start();

// Import Three.js scene (untuk halaman main)
// Akan di-load terpisah di halaman student/main